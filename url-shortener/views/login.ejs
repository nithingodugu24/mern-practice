<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
            background: linear-gradient(115deg, #3e98ff, #2563eb);
            height: 100vh;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
        }

        .container {
            width: 30vw;
            padding: 20px;
            text-align: center;
        }

        h1 {
            color: #fff;
        }


        form {
            display: flex;
            flex-direction: column;
            background-color: #fff;
            color: #000;
            padding: 25px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
            border-radius: 15px;
            width: 100%;
        }

        form input {
            outline: none;
            border: 2px solid #ccc;
            border-radius: 5px;
            height: 40px;
            padding: 8px;
            font-size: 15px;
            font-weight: 600;
            margin-top: 10px;
        }

        form button {
            border: none;
            border-radius: 5px;
            height: 40px;
            padding: 8px;
            font-size: 15px;
            font-weight: 600;
            margin-top: 10px;

            background: linear-gradient(50deg, #2563eb, #3e98ff);
            color: #f2f8ff;
            cursor: pointer;
            background-size: 200% 200%;
            transition: background-position 0.3s ease, all 0.2s ease;

        }

        form button:hover {
            background-position: right center;
        }

        .alert {
            margin: 20px 0 20px 0;
            padding: 10px;
            background-color: rgb(167 0 0 / 20%);
            color: #ffffff;
            border-radius: 10px;
            width: 100%;
            font-size: 15px;
            font-weight: 500;

        }

        button a {
            text-decoration: none;
            color: #fff;
        }
    </style>
</head>

<body>
    <div class="container">
        <% if(locals.error){ %>
            <h3 class="alert">
                <%=locals.error %>
            </h3>
            <% } %>
                <form action="" method="post">
                    <h2>Login page</h2>
                    <input type="email" name="email" placeholder="Enter email" required>
                    <input type="password" name="password" placeholder="Enter password" required>

                    <button type="submit">Login</button>
                    <a href="/signup"><button type="button">No account? Create Account</button></a>
                </form>
    </div>
</body>

</html>